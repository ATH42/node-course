// basic logic to to show how to run node cli

const amount = 9;

console.log(amount);

if (amount < 10) {
  console.log(`${amount} is less than 10`);
}

if (amount > 10) {
  console.log(amount + "is more than 10");
}

console.log(`end of program`);
